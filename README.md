# AI_Resume_Analyzer
AI Resume Analyzer is a smart application that automates the resume screening process using natural language processing (NLP), machine learning (ML), and sometimes large language models (LLMs).
